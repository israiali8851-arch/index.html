import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.israiali88.sharepal',
  appName: 'SharePal Pro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;

