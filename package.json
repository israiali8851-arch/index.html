{
  "name": "sharepal-pro",
  "version": "1.0.0",
  "description": "Offline Sharing App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
